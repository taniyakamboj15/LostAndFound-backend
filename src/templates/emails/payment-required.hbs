<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pay Recovery Fee</title>
  <style>
    body { margin: 0; padding: 0; background: #f4f6f8; font-family: 'Segoe UI', Arial, sans-serif; }
    .wrapper { max-width: 580px; margin: 32px auto; background: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 24px rgba(0,0,0,0.10); }
    .header { background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%); padding: 40px 40px 32px; text-align: center; }
    .header h1 { color: #ffffff; font-size: 26px; font-weight: 700; margin: 0; }
    .header p { color: rgba(255,255,255,0.85); margin: 8px 0 0; font-size: 15px; }
    .body { padding: 36px 40px; }
    .greeting { font-size: 17px; color: #1f2937; margin-bottom: 16px; }
    .status-box { background: #f0fdf4; border: 1px solid #86efac; border-radius: 8px; padding: 16px 20px; margin-bottom: 24px; display: flex; align-items: center; gap: 12px; }
    .status-box span { color: #15803d; font-weight: 600; font-size: 15px; }
    .fee-table { width: 100%; border-collapse: collapse; margin-bottom: 28px; }
    .fee-table td { padding: 11px 14px; border-bottom: 1px solid #e5e7eb; font-size: 15px; color: #374151; }
    .fee-table td:last-child { text-align: right; font-weight: 600; color: #1f2937; }
    .fee-table tr.total td { background: #f9fafb; font-weight: 700; font-size: 16px; color: #111827; border-bottom: none; }
    .cta-btn { display: block; width: fit-content; margin: 0 auto 28px; background: #4f46e5; color: #ffffff; text-decoration: none; padding: 14px 36px; border-radius: 8px; font-size: 16px; font-weight: 600; letter-spacing: 0.3px; }
    .note { font-size: 13px; color: #6b7280; text-align: center; line-height: 1.6; }
    .footer { background: #f9fafb; padding: 20px 40px; text-align: center; font-size: 12px; color: #9ca3af; border-top: 1px solid #e5e7eb; }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="header">
      <h1>🎉 Claim Verified!</h1>
      <p>One last step before you can pick up your item</p>
    </div>
    <div class="body">
      <p class="greeting">Hi {{claimantName}},</p>
      <p style="color:#374151;font-size:15px;line-height:1.6;">
        Great news — your claim for <strong>{{itemDescription}}</strong> has been
        <strong style="color:#15803d;">verified</strong>. To schedule your pickup, please pay
        the recovery fee below.
      </p>

      <div class="status-box">
        <span>✅ Claim Verified — Pay to schedule pickup</span>
      </div>

      <table class="fee-table">
        <tr>
          <td>Handling Fee</td>
          <td>₹{{handlingFee}}</td>
        </tr>
        <tr>
          <td>Storage Fee ({{daysStored}} days × ₹5)</td>
          <td>₹{{storageFee}}</td>
        </tr>
        <tr class="total">
          <td>Total Due</td>
          <td>₹{{totalAmount}}</td>
        </tr>
      </table>

      <a class="cta-btn" href="{{clientUrl}}/claims/{{claimId}}">Pay ₹{{totalAmount}} Now</a>

      <p class="note">
        This fee covers handling and storage costs incurred during safekeeping of your item.<br/>
        Once paid, you'll be able to schedule a convenient pickup slot.
      </p>
    </div>
    <div class="footer">
      © {{year}} Lost & Found System &nbsp;·&nbsp; This is an automated message, please do not reply.
    </div>
  </div>
</body>
</html>
